from pygame import *


win_width = 700
win_height = 500

clock = time.Clock()
FPS = 55
clock.tick(FPS)


game = True
while game:

    for e in event.get():
        keys_pressed = key.get_pressed()
        if e.type == QUIT:
            game = False
        
        display.update()